import 'styles/base.less';
import './index.less';
import utils from 'scripts/utils';

const mermaid = require('mermaid/dist/mermaid');
const win = window;
const doc = document;
utils.loadStyle(utils.getCurScriptPath() + 'index.css');